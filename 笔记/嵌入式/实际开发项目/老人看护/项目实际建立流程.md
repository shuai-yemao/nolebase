## 1. 测试 beep 是否正常

1. Cubemx 将 IO 配置成输入模式，修改为输出模式即可

## 2 .测试按键

1. 短按长按无问题

## 3 .串口测试输出和接收

1. 收发无问题

## 4. OLED 显示

1. 连接引脚，显示正常
2. 菜单运行正常

## 5. MAX 03103

1. IO 配置为开漏上拉高速
2. 移植代码，修改 myiic 引脚编号和头文件
3. 修改 max 30102 头文件
4. 修改 algorithm 头文件
5. 测试代码是否可用
6. 可以正常检测心率和血氧

## 6. Adx 1345

1. 配置 IO 为开漏上拉高速
2. 移植代码修改 iic 标准库代码为 HAL 库模式
3. 测试 iic 通信，SCL 引脚无电平输出
	1. GPIOA 未初始化？已初始化
	2. 电压表测电压，只有 0.3 至 1 v
	3. 连接逻辑分析仪，分析
	4. 通道无输出？电压等级设置到 1.2 v 可以查看
4. 通信无反应？CS 引脚必须连 vdd（由数据手册可知），ALT ADDRESS 引脚必须连接至任一 VDD I/O 或接地 ![[Pasted image 20260415181117.png]]
5. 可以正常输出三轴数据

## 7. GPS/4 G 模块

1. 连接电脑连接模块
2. 使用 AT 指令测试模块是否正常工作
3. 打开 GPS 定位功能 ![[Pasted image 20260415212150.png]]
4. 打开 4 g 联网功能并连接阿里云平台 ![[Pasted image 20260415213008.png]]
5. 开启 GPS 报错 504（已开启 GPS） ![[Pasted image 20260416144333.png]]
6. 定位失败需要在室外测试
7. Mqtt 服务器连接失败，模块显示未联网，模块 VIN 单独供电，且绿灯长亮短闪就是网络正常
	1. 测试开发板与 4 g 模块通信正常，可以连接 emqx 服务器

## 8. RGB 灯

1. Cubemx 配置定时器通道
2. 编写. H 文件的 RGB 结构体
3. 编写 RGB 初始化函数并测试
4. 编写 RGB 设置颜色函数并测试

## 9. DHT 22

1. 移植模块代码
2. 修改标准库代码为 HAL 库
3. 修改 IO 输入输出函数
4. 测试数据读取是否正常

## 10. GY 614

1. 移植代码
2. 修改宏定义开启模拟 iic
3. 查看 IIC 器件地址是否正确
4. 接收 SDA 引脚未拉低，是因为 PS 未焊接？

## 11. HX 711 模块

1. 移植代码
2. 修改标准库函数
3. 修改输入输出函数
4. 测试是否可以正常读取数据
