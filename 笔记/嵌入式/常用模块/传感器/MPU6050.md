rev == 7 时候
![[Pasted image 20251103202511.png]]
加入判断中
MPU6050读取地址为0XFF？

MPU6050加速度，角速度读出来都为0xff？
MPU6050 默认处于**睡眠模式**（PWR_MGMT_1 寄存器 0x6B 的 BIT6=1），若未唤醒，模块不输出数据，读取时返回 0xFF。  
**解决代码**：必须在读取数据前，向 0x6B 寄存器写入 0x00（清睡眠位），示例如下（以 Arduino Wire 库为例）：