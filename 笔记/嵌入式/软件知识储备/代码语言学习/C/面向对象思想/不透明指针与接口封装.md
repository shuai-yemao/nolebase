## 什么是不透明指针 ？

Typedef struct Door Door; //这里是一个前向声明（存放在. H 中，实际结构体在. C 文件中），告诉编译器这里有一个结构体 Door，但是不会告诉它结构体里有什么

前向声明是不透明指针的核心

## 一个好的接口函数 （API）设计要求？

简洁，安全，易用

同时 API 的生命周期为创建，使用，销毁

其中需要注意谁创建谁销毁，为了防止内存泄露（程序中**不再使用的内存没有被正常释放，一直占用系统内存**，就是内存泄漏。）

![[opaque_pointer_tutorial_(1).html]]
